import React from 'react';

const Dashboard = () => {
  // Fetch data from backend API (e.g., seller's store info, inventory)

  return (
    <div style={{ maxWidth: '600px', margin: '0 auto' }}>
      <h2 style={{ textAlign: 'center' }}>Dashboard</h2>
      {/* Display relevant options, forms, and information */}
    </div>
  );
};

export default Dashboard;
